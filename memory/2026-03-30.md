# 2026-03-30 Daily Notes

## 00:00 - 22:54 Cron/插件/飞书通道修复（当晚工作）

### 已完成

1. **清理重复插件（6个）**
   - Agent Browser ×3（clawdbot/bb-browser/fast-browser-use）→ 保留原版
   - Tavily ×1（openclaw-tavily-search）→ 保留 tavily-search
   - claw-router.disabled + openclaw-lark.bak + 临时安装文件
   - Skills: 75→69, Extensions: 10→8

2. **修复全部 48 个 cron 任务的 delivery 配置**
   - 根因：添加 feishu 渠道后，系统要求每个 cron 任务必须指定 `delivery.channel` + `delivery.to`
   - 修复：全部指定 `channel: "feishu"`, `to: "ou_a6469ccc2902a590994b6777b9c8ae8f"`
   - 方法：先用 `cron edit`（失败）→ 直接编辑 jobs.json（成功）

3. **飞书连接健康配置**
   - `channels.feishu.heartbeat.intervalMs`: 30000
   - `channels.feishu.heartbeat.visibility`: true
   - 飞书连接监控 cron：每 30 分钟检测，异常自动重启 gateway

4. **创建自愈工具体系**
   - `scripts/cron_healthcheck.py` — 诊断 cron delivery 配置缺失，`--fix` 自动修复
   - `scripts/self_heal.py` — 综合自愈系统（扫描+修复+学习），覆盖：cron error、delivery 缺失、飞书断联、gateway 异常、磁盘/内存
   - `scripts/self_heal_registry.json` — 故障记忆库（16 条记录）
   - `scripts/self_heal_log.jsonl` — 运行日志

5. **新增 cron 任务**
   - 自愈系统（`af1c061e`）：每小时 `25 * * * *`，自动扫描+修复+学习
   - 飞书连接监控：`*/30 * * * *`，异常自动重启 gateway
   - Cron 自检修复：每 6 小时运行 cron_healthcheck.py --fix

6. **Gateway 全局配置**
   - `cron.failureDestination`: "feishu:ou_a6469ccc2902a590994b6777b9c8ae8f"
   - `cron.failureAlert.mode`: "summary"
   - `cron.failureAlert.threshold`: 2
   - `cron.failureAlert.cooldownMs`: 3600000

### 根因分析
- 原因：只配了 lightclawbot 一个渠道时，cron 任务无需指定 channel
- 添加 feishu 后，系统无法自动判断投递目标 → 48 个任务全部 error
- 第一次修复（cron edit --channel）不完整，缺少 to 参数
- 第二次修复（cron edit --to）CLI 批量操作不稳定
- 最终修复：直接编辑 jobs.json，一次搞定

### 防复发机制
1. 告警层：failureDestination 全局兜底，error 2 次自动飞书通知
2. 自愈层：self_heal.py 每小时扫描+修复
3. 自检层：cron_healthcheck.py 每 6 小时检查 delivery 配置
4. 监控层：飞书连接每 30 分钟检测
5. 故障库：self_heal_registry.json 记录所有错误模式

### ⚠️ 待验证
- 明早 07:00 起定时任务是否正常投递到飞书
- 飞书连接稳定性是否改善
- 自愈系统 cron 任务是否正常执行
